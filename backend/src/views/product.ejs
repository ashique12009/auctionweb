<%- include('partials/header') %>

<div class="container mt-5">
    <h2 class="mb-4"><%= title %></h2>

    <!-- Search form -->
    <form method="GET" action="/product" class="row mb-4">
        <div class="col-md-8">
            <input type="text" name="search" class="form-control" placeholder="Search product by name" value="<%= search %>">
        </div>
        <div class="col-md-4">
            <button type="submit" class="btn btn-primary w-100">Search</button>
        </div>
    </form>

    <!-- Categories table -->
    <table class="table table-striped table-hover">
        <thead class="table-dark">
            <tr>
                <th>ID</th>
                <th>Category Name</th>
                <th>Parent ID</th>
                <th>Created At</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
            <% if (products.length > 0) { %>
                <% products.forEach(product => { %>
                    <tr>
                        <td><%= product.item_id %></td>
                        <td>
                            <form method="POST" action="/product/edit/<%= product.item_id %>" class="d-flex">
                                <input type="text" name="product_name" class="form-control form-control-sm me-2" value="<%= product.title %>">
                                <input type="hidden" name="item_id" value="<%= product.item_id || '' %>">
                                <button type="submit" class="btn btn-sm btn-success">Save</button>
                            </form>
                        </td>
                        <td><%= product.item_id || "-" %></td>
                        <td><%= product.created_at.toISOString().slice(0,19).replace('T',' ') %></td>
                        <td>
                            <form method="POST" action="/product/delete/<%= product.item_id %>" onsubmit="return confirm('Are you sure?')">
                                <button type="submit" class="btn btn-sm btn-danger">Delete</button>
                            </form>
                        </td>
                    </tr>
                <% }) %>
            <% } else { %>
                <tr>
                    <td colspan="5" class="text-center text-muted">No products found</td>
                </tr>
            <% } %>
        </tbody>
    </table>
</div>

<%- include('partials/footer') %>